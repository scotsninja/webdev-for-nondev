Pseudo:
Title page
	introduce presentation
	date
	version
introduce
	jumbled acronyms
	let's just get this over with
dichotomies
	front vs back
	client vs server
	false dichotomies
		technologies blend the two realms, allow them to interact
this is the front
	HTML, CSS, JS, DOM, UI, UX, SASS, LESS, Flash
this is the back
	PHP, .NET, Ruby, SSI, SQL, DBMS, ROR
this is the server
	Apache, IIS, cron, DNS, TCP, IP, TLD, URL, URI, LAMP, ICANN, CNAME, SLD, subdomain
this is the superhighway
	API, SOAP, oAuth, JSON, XML, AJAX, FTP, HTTP, SSL, SSH, RSS, Atom, CMS, WYSIWYG, SaaS, XSS
this is the development environment
	SVN, Git, IDE, XAMPP, SDK, W3C, WAI, Section 508, WCAG, MVC
what does siren do
	multiple steps toward user
		back > front > client
closing
	key ideas
	key questions
	open questions
References page
	references
	see more links

========================

/* templates */

template1
	header
		img {siren logo}
	body content
	footer
		block.left {url}
		block.right
			{date updated}
			{version}
	
template2
	header
		img {siren logo}
		title.right Alphabet Soup
	body content
	footer
		block.left {url}
		block.right
			{date updated}
			{version}

/* slides */

slide[0]
	include(template1/header)
	content
		title Alphabet Soup
		subtitle PHP, ASP, CSS, RoR, LAMP, DNS, SVN, JSON, SSI, TDD, RSS, HTTP, XSS, CSRF, WTF?
		block Presented by {name}
	include(template1/footer)
	
slide[1]
	include(template2/header)
	content
		title
		row 
			col
				img {animation of acroynms fading in randomly}
		row
			col
				block
					i Let's just get this over with
	include(template2/footer)
	
slide[2].title
	include(template2/header)
	content
		title Making sense of it all
		subtitle What does what?
	include(template2/footer)
	
slide[3]
	include(template2/header)
	content
		title Making sense of it all
		subtitle The front-end
		row
			col
				block
					li What the user sees
					li What the user interacts with
					li Runs in the user's browser
			col
				img {person on the computer}
	include(template2/footer)

	slide[3][1]
		include(template2/header)
		content
			title Making sense of it all
			subtitle The front-end
			row
				col
					heading Examples
					block
						li HTML (HyperText Markup Language)
						li CSS (Cascading StyleSheets)
						li XML (eXtensible Markup Language
						li Flash
						li JavaScript (+ libraries)
						li DOM (Document Object Model)
						li The browser
						li The client
				col
					img {person on the computer}
		include(template2/footer)
	
slide[4]
	include(template2/header)
	content
		title Making sense of it all
		subtitle The back-end
		row
			col
				block
					li Does everything else
						li Stores the user's data
						li Manipulates the user's data
						li Builds and serves the webpage
					li Runs on a server somewhere
			col
				img {picture of horse with arrow pointing to rear}
	include(template2/footer)
	
	slide[4][1]
		include(template2/header)
		content
			title Making sense of it all
			subtitle The back-end
			row
				col
					heading Examples
					block
						li Programming
							li PHP (PHP: Hypertext Preprocessor)
							li .NET
							li ROR (Ruby on Rails)
							li SSI (Server Side Includes)
							li Perl
							li Java
							li Python
		include(tempalte2/footer)

	slide[4][2]
		include(template2/header)
		content
			title Making sense of it all
			subtitle The back-end
			row
				col
					heading Examples
					block
						li Storage
							li RDBMS (Relational Database Management System)
								li MySQL
								li SQL Server
								li Oracle Database
							li SQL (Structured Query Langauge)
							li NoSQL
								li CouchDB
								li MongoDB
							li XML (eXtensbile Markup Language)
							li INI
				col
					img {picture of horse with arrow pointing to rear}
		include(template2/footer)

	slide[4][3]
		include(template2/header)
		content
			title Making sense of it all
			subtitle The back-end
			row
				col
					heading Examples
					block
						li The Server
							li Apache
							li IIS (Internet Information Services)
							li Node.js
							li crontab
							li LAMP (Linux Apache MySQL PHP)
						li The Web
							li HTTP (HyperText Transfer Protocol)
							li FTP
							li SSL (Secure Sockets Layer)
							li TCP/IP (Transmission Control Protocol/Internet Protocol)
							li DNS (Domain Name System)
								li URL (Universal Resource Locator)
								li URI (Universal Resource Indicator)
								li CNAME (Canonical Name)
								li TLD (Top-Level Domain)
								li SLD (Second-Level Domain)
								li subdomain
				col
					img {picture of horse with arrow pointing to rear}
		include(template2/footer)
		
slide[5]
	include(template2/header)
	content
		title Making sense of it all
		subtitle Tying it all together
		row
			col
				block
					li It's not front-end vs back-end
					li It's not client vs server
					li It's abstraction
					li It's communication
					li It's connection
					li It's the Internet
			col
				img {node graph of Internet connections}
	include(template2/footer)
	
	slide[5][1]
		include(template2/header)
		content
			title Making sense of it all
			subtitle The back-end
			row
				col
					heading Examples
					block
						li CMS (Content Management System)
						li WYSIWYG (What You See Is What You Get)
						li API (Application Programming Interface)
						li SaaS (Software as a Service)
						li REST (Representational State Transfer)
						li SOAP (Simple Object Access Protocol)
						li AJAX (Asynchronous JavaScript and XML)
						li JSON (JavaScript Object Notation)
						li XML (eXtensible Markup Language)
						li RSS/Atom
						li XSS (Cross-site Scripting)
				col
					img {node graph of Internet connections}
		include(template2/footer)

slide[6]
	include(template2/header)
	content
		title Making sense of it all
		subtitle The development
		row
			col
				block
					li Makes everything possible
					li The source code and project requirements
					li Runs on the developer's computer
			col
				img {}
	include(template2/footer)
	
	slide[6][1]
		include(template2/header)
		content
			title Making sense of it all
			subtitle The development
			row
				col
					heading Examples
					block
						li Environment
							li XAMPP
							li Cygwin
							li Version control
						li Coding
							li IDE (Integrated Development Environment)
								li Eclipse
								li Visual Studio
								li NetBeans
							li Text editors
								li Notepad++
								li vi/Vim/Emacs
							li SDK (Software Development Kit)
							li MVC (Model-View-Controller)
				col
					img {}
		include(template2/footer)

slide[7]
	/*
	what does siren do
	multiple steps toward user
		back > front > client
	*/
	include(template2/header)
	content
		title What does Siren use?
		row
			col
				heading Rob/Kyle
			col
				heading Server
			col
				heading Communication
			col
				heading Client
	include(template2/footer)

slide[8]
	/*
	closing
		key ideas
		key questions
		open questions
	*/
	include(template2/header)
	content
	include(template2/footer)
	

slide[]
	include(template2/header)
	content
		title References and Additional Information
		row
			col
				heading See Also
				block
					This presentation can be found at
					{link}
					Notes and source files also available at
					{link}
					{git link}
			col
				heading Credits
				heading Special Thanks
				block
					Presentation platform built with help from the
					li 
						jQuery community
						italic {link}
					li
						Reveal.js
						italic {link}
	include(template2/footer)